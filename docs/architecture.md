# DBTT Architecture

DBTT is a Cognitive Operating System (“Brain OS”).

- The **Thought Graph** is the single system state.
- Every brain module reads/writes structured Thoughts and updates the graph.
- The LLM is only a language generation component.

This document will be expanded as modules are implemented.

